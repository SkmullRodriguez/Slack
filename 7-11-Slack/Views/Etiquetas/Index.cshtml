@model IEnumerable<_7_11_Slack.Models.Etiqueta>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreEtiqueta)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Estado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreEtiqueta)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Estado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdEtiqueta }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdEtiqueta }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdEtiqueta })
        </td>
    </tr>
}

</table>
